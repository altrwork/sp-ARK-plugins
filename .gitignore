# macOS
.DS_Store

# Node dependencies
node_modules/

# Local secrets
.env
.env.*
questions.md

# mcp servers (root-level only — plugin .mcp.json files should be committed)
/.mcp.json

# setup.md
SETUP.md

# shared credential files
**/references/edwin-settings.json

# mcpb build artifacts
*.mcpb

# Cloudflare Worker local secrets + build state
.dev.vars
.wrangler/
